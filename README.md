# Feedibus Datenbank
## v0.1
* Die Pipeline wird bereitgestellt um ein Docker-Image aus dem bestehenden SQL skript zu erzeugen
* Das Docker-Image soll auf einem alpine Linux image basieren
* Das passwort für die Datenbank soll in einem github Secret verwaltet werden
    * Name der Variable: DATABASE_PASSWORD