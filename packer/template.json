{
  "builders": [
    {
      "azure_tags": {
        "dept": "engineering"
      },
      "client_id": "{{user `client_id`}}",
      "client_secret": "{{user `client_secret`}}",
      "image_offer": "UbuntuServer",
      "image_publisher": "Canonical",
      "image_sku": "18.04-LTS",
      "location": "UK South",
      "os_type": "Linux",
      "resource_group_name": "feedibus-production",
      "storage_account": "amistoragefeedibus",
      "subscription_id": "{{user `subscription_id`}}",
      "tenant_id": "{{user `tenant_id`}}",
      "type": "azure-arm",
      "vm_size": "Standard_B1ms",
      "temp_compute_name": "packer-image-provisioner",
      "temp_resource_group_name": "packer-image-provisioner-group",
      "capture_name_prefix": "feedibus-base-image-",
      "async_resourcegroup_delete": true
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "script": "packer/scripts/enable-ansible.sh"
    },
    {
      "type": "ansible-local",
      "playbook_file": "ansible/site.yml",
      "role_paths": [
        "ansible/roles/docker"
      ]
    },
    {
      "type": "shell",
      "script": "packer/scripts/remove-ansible.sh"
    },
    {
      "type": "shell",
      "script": "packer/scripts/enable-docker-compose.sh"
    }
  ],
  "variables": {
    "client_id": "{{env `ARM_CLIENT_ID`}}",
    "client_secret": "{{env `ARM_CLIENT_SECRET`}}",
    "subscription_id": "{{env `ARM_SUBSCRIPTION_ID`}}",
    "tenant_id": "{{env `ARM_TENANT_ID`}}"
  }
}
